

#ifndef SERVER_H
# define SERVER_H

# include <string>
# include <iostream>

class server {
	public:
		server( void );
		~server( void );
		server( server const & copy );
		server & operator=( server const & rhs );
	
	private:

};

#endif // SERVER_H
	